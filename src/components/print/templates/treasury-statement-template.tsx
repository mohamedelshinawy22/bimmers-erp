type TreasuryStatementProps = { name: string; period: string; opening: number; incoming: number; outgoing: number; closing: number; rows: Array<{ at: string; reference: string; description: string; incoming: number; outgoing: number; balance: number }> };
export function TreasuryStatementTemplate({ name, period, opening, incoming, outgoing, closing, rows }: TreasuryStatementProps) {
  return <article className="invoice-print-page invoice-a4" dir="rtl"><header className="invoice-header"><div><h1>كشف حساب الخزينة</h1><p>{name}</p></div><p>{period}</p></header><table className="invoice-lines"><thead><tr><th>التاريخ</th><th>المرجع</th><th>الوصف</th><th>وارد</th><th>منصرف</th><th>الرصيد</th></tr></thead><tbody>{rows.map((row, index) => <tr key={`${row.reference}-${index}`}><td>{row.at}</td><td>{row.reference}</td><td>{row.description}</td><td>{row.incoming.toFixed(2)}</td><td>{row.outgoing.toFixed(2)}</td><td>{row.balance.toFixed(2)}</td></tr>)}</tbody></table><section className="invoice-totals mt-4"><p>رصيد سابق <b>{opening.toFixed(2)}</b></p><p>إجمالي الوارد <b>{incoming.toFixed(2)}</b></p><p>إجمالي المنصرف <b>{outgoing.toFixed(2)}</b></p><p className="grand-total">الرصيد الختامي <b>{closing.toFixed(2)}</b></p></section></article>;
}
