type ShiftReportProps = { storeName: string; period: string; cashier: string; opening: number; net: number; closing: number; rows: Array<{ label: string; count: number; total: number }> };
export function DailyShiftReportTemplate({ storeName, period, cashier, opening, net, closing, rows }: ShiftReportProps) {
  return <article className="invoice-print-page invoice-a4" dir="rtl"><header className="invoice-header"><div><h1>{storeName}</h1><p>تقرير إغلاق وردية — {period}</p></div><div><p>الكاشير: {cashier}</p></div></header><table className="invoice-lines"><thead><tr><th>العملية</th><th>العدد</th><th>الإجمالي</th></tr></thead><tbody>{rows.map((row) => <tr key={row.label}><td>{row.label}</td><td>{row.count}</td><td>{row.total.toFixed(2)}</td></tr>)}</tbody></table><section className="invoice-bottom"><div className="invoice-totals"><p>الرصيد الافتتاحي <b>{opening.toFixed(2)}</b></p><p>صافي الحركة <b>{net.toFixed(2)}</b></p><p className="grand-total">الرصيد الختامي <b>{closing.toFixed(2)}</b></p></div></section><footer className="flex justify-between"><span>المحاسب: ______________</span><span>مدير الفرع: ______________</span></footer></article>;
}
